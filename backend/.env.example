# Backend Environment Variables
# 复制此文件为 .env 并填入你的值

# ===== 必需配置 =====

# AWS 配置
AWS_REGION=us-east-1
AWS_STACK_NAME=zmt-backend
MEDIA_BUCKET=zmt-media-prod
MEDIA_CDN_URL=https://s3.your-domain.com
MEDIA_CDN_DOMAIN=s3.your-domain.com
MEDIA_CERTIFICATE_ARN=arn:aws:acm:us-east-1:ACCOUNT_ID:certificate/CERTIFICATE_ID

# Cognito 用户池 ARN (必需)
# 从 AWS Console > Cognito > User pools > 你的用户池 获取
COGNITO_USER_POOL_ARN=arn:aws:cognito-idp:region:account:userpool/pool-id

# GitHub Personal Access Token (必需)
# 用于 GitHub API 调用和自动发布功能
GITHUB_PAT=your_github_personal_access_token
GITHUB_REPO=ethanlita/zmt

# ===== 可选配置 (用于自定义域名) =====

# ACM 证书 ARN (可选)
# 用于 API Gateway 自定义域名
ACM_CERTIFICATE_ARN=arn:aws:acm:us-east-1:account:certificate/cert-id

# API 自定义域名 (可选)
# 例如: api.zunmingtea.com
API_CUSTOM_DOMAIN=api.example.com

# ===== 自动生成配置 (通常不需要修改) =====

# DynamoDB 表名 (与 template.yaml 一致)
PAGES_TABLE=zmt-pages-prod
PRODUCTS_TABLE=zmt-products-prod
PRODUCT_SERIES_TABLE=zmt-product-series-prod
